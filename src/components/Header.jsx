// src/components/Header.jsx
// Removed — header is now part of AppLayout topbar.
// This file kept for backward compat; pages no longer need to import it.
export default function Header() { return null; }
